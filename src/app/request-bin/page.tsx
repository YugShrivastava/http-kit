import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Toolkit | Bin",
  description: "Request bin of toolkit",
};

export default function RequestBinPage() {
  return (
    <section>
      <h1 className="text-3xl">Request Bin</h1>
    </section>
  )
}